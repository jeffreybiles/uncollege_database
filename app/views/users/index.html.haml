%h1 Listing users

%table
  %tr
    %th Name
    %th Email
    %th City
    %th College
    %th Job

  - @users.each do |user|
    %tr
      %td= link_to user.name, user
      %td= user.email
      %td= user.city
      %td= user.college
      %td= user.job
      %td= link_to 'Edit', edit_user_path(user) if can? :update, user
      %td= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete if can? :delete, user

%br
