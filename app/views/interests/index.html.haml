%h1 Listing interests

%table
  %tr
    %th User
    %th Activity
    %th Notes
    %th Mentor?
    %th Mentee?
    %th
    %th
    %th

  - @interests.each do |interest|
    %tr
      %td= link_to interest.user.name, user_path(interest.user_id)
      %td= link_to interest.activity.name, activity_path(interest.activity_id)
      %td= interest.notes
      %td= interest.wants_to_mentor
      %td= interest.wants_to_be_mentored
      %td= link_to 'Show', interest
      %td= link_to 'Edit', edit_interest_path(interest)
      %td= link_to 'Destroy', interest, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Interest', new_interest_path
